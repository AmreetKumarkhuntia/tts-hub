"""TTS engine implementations.

Engines are concrete TTSEngine subclasses. Registration lives in app/bootstrap.py
(not here) so this package has no dependency on the registry, keeping imports acyclic.
"""
